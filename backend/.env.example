# CarWiseIQ Backend - Environment Variables
# Copy to .env and set values. Never commit .env to git.

# Environment: development | production
ENV=development
DEBUG=true

# API
HOST=127.0.0.1
PORT=8000

# JWT - REQUIRED in production. Generate with: python scripts/generate_secret.py
# Minimum 256-bit (32 characters) for production.
SECRET_KEY=your-secret-key-min-32-chars
JWT_ACCESS_EXPIRE_MINUTES=15
JWT_REFRESH_EXPIRE_DAYS=7
JWT_ISSUER=carwiseiq-api
JWT_AUDIENCE=carwiseiq-app

# CORS - Production: only your domains
# Development: leave default or add localhost
CORS_ORIGINS=https://carwiseiq.com,https://www.carwiseiq.com

# Supabase (for JWT verification and listings)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_JWT_SECRET=your-supabase-jwt-secret
SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: Anthropic Claude (image labeling)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=

# Optional: Car classifier model paths
# CAR_CLASSIFIER_MODEL=
# CAR_CLASSIFIER_LABEL_MAPS=
